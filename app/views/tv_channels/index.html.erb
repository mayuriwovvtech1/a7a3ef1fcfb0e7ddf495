<p><%= link_to('Logout', destroy_user_session_path, method: :delete) %></p>
<h2>Welcome, <%= @user.email %></h2> 

<% @channels.each do |chal|%>
	<p><li><%= link_to chal.name, tv_channel_path(chal.id) %></li></p>
<% end %>